[project]
name = "autoresearch-plus"
version = "0.1.0"
description = "A minimal autoresearch-style experiment loop with ledger, baseline gate, and traces."
requires-python = ">=3.11"

[tool.autoresearch_plus]
package = "autoresearch_plus"
